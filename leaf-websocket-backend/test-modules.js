console.log('🧪 Testando importação de módulos...\n');

try {
    console.log('1. Testando LatencyMonitor...');
    const LatencyMonitor = require('./metrics/latency-monitor');
    console.log('✅ LatencyMonitor importado:', typeof LatencyMonitor);
    
    const latencyMonitor = new LatencyMonitor();
    console.log('✅ LatencyMonitor instanciado:', typeof latencyMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar LatencyMonitor:', error.message);
}

try {
    console.log('\n2. Testando DockerMonitor...');
    const DockerMonitor = require('./monitoring/docker-monitor');
    console.log('✅ DockerMonitor importado:', typeof DockerMonitor);
    
    const dockerMonitor = new DockerMonitor();
    console.log('✅ DockerMonitor instanciado:', typeof dockerMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar DockerMonitor:', error.message);
}

try {
    console.log('\n3. Testando SmartSyncAlertSystem...');
    const SmartSyncAlertSystem = require('./monitoring/smart-sync-alert-system');
    console.log('✅ SmartSyncAlertSystem importado:', typeof SmartSyncAlertSystem);
    
    const smartSyncAlertSystem = new SmartSyncAlertSystem();
    console.log('✅ SmartSyncAlertSystem instanciado:', typeof smartSyncAlertSystem);
    
} catch (error) {
    console.log('❌ Erro ao importar SmartSyncAlertSystem:', error.message);
}

try {
    console.log('\n4. Testando firebase-config...');
    const firebaseConfig = require('./firebase-config');
    console.log('✅ firebase-config importado:', typeof firebaseConfig);
    console.log('✅ Métodos disponíveis:', Object.keys(firebaseConfig));
    
} catch (error) {
    console.log('❌ Erro ao importar firebase-config:', error.message);
}

console.log('\n🎯 Teste concluído!'); 

try {
    console.log('1. Testando LatencyMonitor...');
    const LatencyMonitor = require('./metrics/latency-monitor');
    console.log('✅ LatencyMonitor importado:', typeof LatencyMonitor);
    
    const latencyMonitor = new LatencyMonitor();
    console.log('✅ LatencyMonitor instanciado:', typeof latencyMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar LatencyMonitor:', error.message);
}

try {
    console.log('\n2. Testando DockerMonitor...');
    const DockerMonitor = require('./monitoring/docker-monitor');
    console.log('✅ DockerMonitor importado:', typeof DockerMonitor);
    
    const dockerMonitor = new DockerMonitor();
    console.log('✅ DockerMonitor instanciado:', typeof dockerMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar DockerMonitor:', error.message);
}

try {
    console.log('\n3. Testando SmartSyncAlertSystem...');
    const SmartSyncAlertSystem = require('./monitoring/smart-sync-alert-system');
    console.log('✅ SmartSyncAlertSystem importado:', typeof SmartSyncAlertSystem);
    
    const smartSyncAlertSystem = new SmartSyncAlertSystem();
    console.log('✅ SmartSyncAlertSystem instanciado:', typeof smartSyncAlertSystem);
    
} catch (error) {
    console.log('❌ Erro ao importar SmartSyncAlertSystem:', error.message);
}

try {
    console.log('\n4. Testando firebase-config...');
    const firebaseConfig = require('./firebase-config');
    console.log('✅ firebase-config importado:', typeof firebaseConfig);
    console.log('✅ Métodos disponíveis:', Object.keys(firebaseConfig));
    
} catch (error) {
    console.log('❌ Erro ao importar firebase-config:', error.message);
}

console.log('\n🎯 Teste concluído!'); 

try {
    console.log('1. Testando LatencyMonitor...');
    const LatencyMonitor = require('./metrics/latency-monitor');
    console.log('✅ LatencyMonitor importado:', typeof LatencyMonitor);
    
    const latencyMonitor = new LatencyMonitor();
    console.log('✅ LatencyMonitor instanciado:', typeof latencyMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar LatencyMonitor:', error.message);
}

try {
    console.log('\n2. Testando DockerMonitor...');
    const DockerMonitor = require('./monitoring/docker-monitor');
    console.log('✅ DockerMonitor importado:', typeof DockerMonitor);
    
    const dockerMonitor = new DockerMonitor();
    console.log('✅ DockerMonitor instanciado:', typeof dockerMonitor);
    
} catch (error) {
    console.log('❌ Erro ao importar DockerMonitor:', error.message);
}

try {
    console.log('\n3. Testando SmartSyncAlertSystem...');
    const SmartSyncAlertSystem = require('./monitoring/smart-sync-alert-system');
    console.log('✅ SmartSyncAlertSystem importado:', typeof SmartSyncAlertSystem);
    
    const smartSyncAlertSystem = new SmartSyncAlertSystem();
    console.log('✅ SmartSyncAlertSystem instanciado:', typeof smartSyncAlertSystem);
    
} catch (error) {
    console.log('❌ Erro ao importar SmartSyncAlertSystem:', error.message);
}

try {
    console.log('\n4. Testando firebase-config...');
    const firebaseConfig = require('./firebase-config');
    console.log('✅ firebase-config importado:', typeof firebaseConfig);
    console.log('✅ Métodos disponíveis:', Object.keys(firebaseConfig));
    
} catch (error) {
    console.log('❌ Erro ao importar firebase-config:', error.message);
}

console.log('\n🎯 Teste concluído!'); 
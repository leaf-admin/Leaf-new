// Deprecated compatibility bridge.
// Keep legacy imports alive while the canonical Firebase config lives under
// services/canonical.
export { firebase } from '../../services/canonical/firebaseConfig';
